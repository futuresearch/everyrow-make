# Make.com API credentials
# Get your API key from: Make.com → Profile → API → Create token (with apps:write scope)
MAKE_API_KEY=your-make-api-key-here

# The app ID/name in Make.com (create the app first in Make.com → Apps → Create a new App)
MAKE_APP_ID=everyrow

# App version (optional, defaults to 1)
MAKE_APP_VERSION=1

# Make.com region (optional, defaults to us1)
# Options: us1.make.com, us2.make.com, eu1.make.com, eu2.make.com
MAKE_BASE_URL=https://us1.make.com/api
